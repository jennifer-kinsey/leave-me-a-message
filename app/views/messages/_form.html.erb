<%= render "layouts/errors", :object => @message %>

<%= form_for [@user, @message] do |f| %>
  <%= f.text_area(:body, size: "100x15", required: true, placeholder: "hey #{@user.username}, you are awesome!") %>
  <br>
  <%= f.submit :Submit, class: 'btn btn-success' %>
<% end %>
